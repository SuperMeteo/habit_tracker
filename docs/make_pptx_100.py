# -*- coding: utf-8 -*-
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from _deck_lib import *

HERE = os.path.dirname(os.path.abspath(__file__))
TPL = r"C:\Users\Admin\AppData\Roaming\Microsoft\Windows\Network Shortcuts\Presentation.fixed.pptx"
SHOT = os.path.join(HERE, "shots100")

prs = blank_from(TPL)
DEFAULT, MASTER = prs.slide_layouts[0], prs.slide_layouts[1]


def dark():
    return prs.slides.add_slide(DEFAULT)


def page():
    return prs.slides.add_slide(MASTER)


def shot(name):
    return os.path.join(SHOT, name)


# ---------------------------------------------------------------- 1 cover
s = dark()
box(s, 0, 0, 13.33, 7.5, NAVY, None, MSO_SHAPE.RECTANGLE, None)
box(s, 8.7, -1.2, 6.0, 6.0, NAVY2, None, MSO_SHAPE.OVAL, None)
box(s, 9.95, 0.85, 2.35, 2.35, NAVY2, None, MSO_SHAPE.OVAL, None)
txt(s, 9.92, 1.42, 2.4, 0.6, "100%", 36, True, WHITE, PP_ALIGN.CENTER)
txt(s, 9.94, 2.04, 2.4, 0.62, "PROJECT\nPROGRESS", 11, True, ICE, PP_ALIGN.CENTER, font="Aptos")
txt(s, 0.8, 1.25, 7.6, 1.55, "นำเสนอผลงานโครงงาน\nความสำเร็จ 100%", 31, True, WHITE, line=1.25)
txt(s, 0.82, 3.12, 8.5, 0.45, "Habit Tracker — Daily Habit Tracking Mobile Application", 14.5, False, PALE)
box(s, 0.82, 4.1, 7.25, 1.55, NAVY3, None, adj=0.07)
txt(s, 1.1, 4.42, 6.7, 1.0,
    "ชื่อโครงงาน:  แอปพลิเคชันติดตามพฤติกรรมประจำวันบนมือถือ\n"
    "ผู้จัดทำ:  ภูตะวัน ทองวิลาศ     รหัสนักศึกษา: 68409010016\n"
    "อาจารย์ที่ปรึกษา:  นฐมนพรรณ สุวรรณชาตรี", 15.5, False, WHITE, line=1.3, space=6)
txt(s, 0.82, 6.65, 10.6, 0.35,
    "ติดตั้งจริงด้วยไฟล์ APK · ทดสอบอัตโนมัติผ่าน 45 กรณี  ·  วิทยาลัยเทคนิคเชียงใหม่", 12.5, False, ICE)

# ---------------------------------------------------------------- 2 problem
s = page()
head(s, "01", "ปัญหาและความจำเป็นของโครงงาน",
     "คนตั้งใจสร้างนิสัยใหม่แล้วเลิกกลางคัน เพราะไม่เห็นความต่อเนื่องของตัวเอง",
     "เลิกกลางคัน · ดูย้อนหลังไม่ได้ · ต้องต่อเน็ต")
card(s, 0.70, 1.45, 3.75, 2.05, RED, "ปัญหาหลัก",
     "• จดในสมุดหรือโน้ตมือถือ สรุปย้อนหลังไม่ได้\n"
     "• ไม่รู้ว่าทำต่อเนื่องมากี่วัน จึงขาดแรงจูงใจ\n"
     "• แอปในตลาดต้องสมัครสมาชิกและต่ออินเทอร์เน็ต", 11.5, num="1")
card(s, 4.80, 1.45, 3.75, 2.05, AMBER, "ผู้ใช้กลุ่มเป้าหมาย",
     "• นักเรียน–นักศึกษาที่ต้องการสร้างวินัย\n"
     "• วัยทำงานที่ติดตามพฤติกรรมสุขภาพ\n"
     "• ผู้ที่ไม่ต้องการฝากข้อมูลส่วนตัวไว้บนคลาวด์", 11.5, num="2")
card(s, 8.90, 1.45, 3.75, 2.05, TEAL, "แนวทางที่พัฒนา",
     "แอปบนมือถือที่เก็บข้อมูลในเครื่องด้วย SQLite ใช้งานออฟไลน์ "
     "ไม่ต้องสมัครสมาชิก คำนวณวันต่อเนื่องและอัตราสำเร็จอัตโนมัติ "
     "พร้อมแจ้งเตือนตามเวลาที่ตั้งไว้", 11.5, num="3")
note(s, 0.70, 3.90, 11.95, 1.75, "วิธีเดิม",
     "จดในสมุดหรือแอปโน้ต → ไม่มีการคำนวณย้อนหลัง → ไม่รู้ว่าขาดไปกี่วัน → เลิกทำ\n"
     "แอปสำเร็จรูปส่วนใหญ่ต้องสมัครสมาชิก ต่ออินเทอร์เน็ต และเป็นภาษาอังกฤษ\n"
     "ผลคือผู้ใช้ไม่เห็น “ความต่อเนื่อง” ซึ่งเป็นแรงจูงใจสำคัญที่สุดของการสร้างนิสัย")

# ---------------------------------------------------------------- 3 objectives
s = page()
head(s, "02", "วัตถุประสงค์และเกณฑ์ความสำเร็จ",
     "วัตถุประสงค์ 3 ข้อ ตรวจผลได้จากแอปจริงและผลการทดสอบอัตโนมัติ",
     "ทุกข้อมีตัวชี้วัดที่ตรวจได้ในวันสอบ")
for i, (t, d) in enumerate([
        ("วัตถุประสงค์ข้อ 1", "บันทึกพฤติกรรมประจำวันแบบออฟไลน์ ไม่ต้องสมัครสมาชิก"),
        ("วัตถุประสงค์ข้อ 2", "คำนวณสถิติ Streak อัตราสำเร็จรายสัปดาห์ และปฏิทินย้อนหลัง 1 ปี"),
        ("วัตถุประสงค์ข้อ 3", "รองรับความถี่หลายแบบ แจ้งเตือนตามเวลา และส่งออกข้อมูลได้")]):
    y = 1.50 + i * 1.32
    box(s, 0.70, y, 7.05, 1.05, WHITE, BORDER, adj=0.09)
    txt(s, 0.98, y + 0.36, 2.1, 0.3, t, 14, True, NAVY)
    txt(s, 3.05, y + 0.22, 4.45, 0.75, d, 12.2, False, INK, line=1.3)
box(s, 8.05, 1.50, 4.60, 3.87, RGBColor(0xF2, 0xF7, 0xFB), RGBColor(0xC9, 0xDA, 0xE8), adj=0.07)
txt(s, 8.35, 1.85, 4.0, 0.34, "Success Criteria / ตัวชี้วัด", 16, True, NAVY)
txt(s, 8.35, 2.45, 4.0, 2.6,
    "• ฟังก์ชันตามขอบเขตครบ 9/9 รายการ\n"
    "• ชุดทดสอบอัตโนมัติผ่านทุกกรณี\n"
    "• ใช้งานได้ขณะปิดอินเทอร์เน็ต\n"
    "• ปิดแอปแล้วเปิดใหม่ ข้อมูลไม่หาย\n"
    "• สร้างไฟล์ APK ติดตั้งบนมือถือได้จริง", 12.2, False, INK, line=1.5, space=5)
txt(s, 0.70, 5.85, 11.95, 0.35,
    "เกณฑ์: ฟังก์ชัน 9/9  |  flutter test 45/45  |  ใช้งานออฟไลน์  |  APK ติดตั้งได้",
    14, True, NAVY, PP_ALIGN.CENTER)

# ---------------------------------------------------------------- 4 scope
s = page()
head(s, "03", "ขอบเขตระบบและฟังก์ชันที่ส่งมอบ",
     "ทุกฟังก์ชันที่เสนอไว้พัฒนาเสร็จครบ พร้อมหลักฐานตรวจสอบ",
     "ครบตามขอบเขต 9/9 รายการ")
txt(s, 0.70, 1.38, 4.0, 0.3, "ตารางสถานะฟังก์ชัน", 15.5, True, NAVY)
table(s, 0.70, 1.75, 8.0,
      ["ฟังก์ชัน / โมดูล", "สถานะ", "หลักฐาน"],
      [["1. ฐานข้อมูล SQLite 3 ตาราง (drift)", ("100%", GREEN, True), "app_database.dart + .g.dart"],
       ["2. หน้าหลัก บันทึกพฤติกรรมรายวัน", ("100%", GREEN, True), "ภาพหน้าจอ · สาธิตสด"],
       ["3. เพิ่ม–แก้ habit + แม่แบบ 18 รายการ", ("100%", GREEN, True), "add_habit · template_picker"],
       ["4. สถิติ Streak · ปฏิทิน 1 ปี · กราฟ", ("100%", GREEN, True), "analytics_screen + fl_chart"],
       ["5. ธีมสว่าง / มืด และจำค่าไว้", ("100%", GREEN, True), "settings + shared_preferences"],
       ["6. แจ้งเตือนตามเวลาที่ตั้งไว้", ("100%", GREEN, True), "notification_service + test"],
       ["7. ความถี่ “N ครั้งต่อสัปดาห์”", ("100%", GREEN, True), "weekly_frequency_test"],
       ["8. จัดการหมวดหมู่ + ส่งออก CSV / JSON", ("100%", GREEN, True), "manage_categories · exporter"],
       ["9. ชุดทดสอบอัตโนมัติ", ("100%", GREEN, True), "6 ไฟล์ · 45 กรณี ผ่านหมด"]],
      [4.3, 1.0, 2.7], row_h=0.43, body_size=10.5)
card(s, 9.05, 1.75, 3.60, 2.35, TEAL, "ขอบเขตข้อมูล / ผู้ใช้",
     "• ใช้งานคนเดียวบนเครื่องตัวเอง ไม่มีระบบสมาชิก\n"
     "• เก็บข้อมูลในเครื่องด้วย SQLite\n"
     "• ผลลัพธ์: สถิติในแอป + ไฟล์ CSV / JSON\n"
     "• ภาษาไทยทั้งแอป + ธีมสว่างและมืด", 11, title_size=14)
card(s, 9.05, 4.28, 3.60, 2.05, AMBER, "อยู่นอกขอบเขต",
     "• ระบบสมาชิกและการซิงก์ขึ้นคลาวด์\n"
     "• การแชร์ข้อมูลระหว่างผู้ใช้\n"
     "• เวอร์ชัน iOS (โค้ดรองรับ แต่ไม่ได้ทดสอบ)", 11, title_size=14)

# ---------------------------------------------------------------- 5 design
s = page()
head(s, "04", "การออกแบบระบบ / Architecture",
     "แยกงานเป็น 4 ชั้น หน้าจอเฝ้าดูฐานข้อมูล ข้อมูลเปลี่ยนแล้วทุกหน้าที่เกี่ยวข้องอัปเดตเอง",
     "UI → สถานะ → ข้อมูล → SQLite")
layers = [("ชั้นแสดงผล (UI) — Flutter Widgets",
           "หน้าหลัก · สถิติ · เพิ่ม/แก้ habit · แม่แบบ · จัดการหมวดหมู่ · ตั้งค่า   |   นำทางด้วย go_router",
           RGBColor(0x2E, 0x75, 0xB6)),
          ("ชั้นสถานะและตรรกะ — Riverpod",
           "habitsProvider · dashboardProvider · analyticsProvider · heatmapProvider   |   StreakCalculator",
           TEAL),
          ("ชั้นข้อมูล — AppDatabase (drift ORM)",
           "watchActiveHabits() · watchLogsForDate() · upsertLog() · getAllLogsForHabit()",
           RGBColor(0x6D, 0x4A, 0xAF)),
          ("ฐานข้อมูล SQLite ในเครื่อง — habit_tracker.db",
           "Categories (1) —< Habits (1) —< HabitLogs   |   คีย์ไม่ซ้ำ: (habitId, loggedDate)",
           RGBColor(0x3F, 0x4B, 0x58))]
box(s, 0.70, 1.45, 7.05, 4.70, WHITE, BORDER, adj=0.04)
for i, (t, d, c) in enumerate(layers):
    y = 1.75 + i * 1.12
    box(s, 0.95, y, 6.55, 0.88, c, None, adj=0.12)
    txt(s, 1.10, y + 0.15, 6.25, 0.28, t, 12.5, True, WHITE, PP_ALIGN.CENTER)
    txt(s, 1.10, y + 0.47, 6.25, 0.34, d, 8.8, False, RGBColor(0xE4, 0xEE, 0xF7), PP_ALIGN.CENTER, line=1.1)
    if i < 3:
        a = s.shapes.add_shape(MSO_SHAPE.DOWN_ARROW, Inches(4.05), Inches(y + 0.92), Inches(0.28), Inches(0.18))
        a.fill.solid()
        a.fill.fore_color.rgb = RGBColor(0xA8, 0xB6, 0xC4)
        a.line.fill.background()
        a.shadow.inherit = False
card(s, 8.05, 1.45, 4.60, 4.70, TEAL, "เครื่องมือและเหตุผลที่เลือก",
     "• Flutter 3.44 + Dart — เขียนชุดเดียวได้ทั้ง Android และเว็บ\n"
     "• drift 2.21 + SQLite — ตรวจชนิดข้อมูลตั้งแต่ตอนคอมไพล์ ลดบั๊กคำสั่งค้นข้อมูล\n"
     "• flutter_riverpod 2.6 — หน้าจออัปเดตเองเมื่อข้อมูลเปลี่ยน\n"
     "• go_router 14.6 · fl_chart 0.69 · intl · shared_preferences\n"
     "• flutter_local_notifications 18 + timezone — ตั้งเวลาเตือนซ้ำทุกวัน\n"
     "• share_plus 13 — ส่งออกไฟล์ CSV / JSON ออกจากแอป\n\n"
     "เหตุผลหลัก: เก็บข้อมูลในเครื่อง = ใช้งานได้แม้ไม่มีอินเทอร์เน็ต ไม่ต้องมีเซิร์ฟเวอร์ "
     "และข้อมูลส่วนตัวไม่ออกจากเครื่องผู้ใช้", 11.2, title_size=15)

# ---------------------------------------------------------------- 6 method
s = page()
head(s, "05", "วิธีดำเนินการพัฒนา",
     "พัฒนาตั้งแต่ 4 มิถุนายน ถึง 16 กันยายน 2569 มีหลักฐานจาก Git ทุกขั้น",
     "หลักฐานจาก Git และชุดทดสอบ")
steps = [("1", "Requirement", "เก็บปัญหาผู้ใช้ที่เลิกทำนิสัยกลางคัน"),
         ("2", "Design", "ออกแบบ 3 ตาราง และสถาปัตยกรรม 4 ชั้น"),
         ("3", "Develop", "Flutter + drift + Riverpod · 9 หน้าจอ"),
         ("4", "Feature", "แจ้งเตือน · N ครั้ง/สัปดาห์ · ส่งออกข้อมูล"),
         ("5", "Test", "ชุดทดสอบ 45 กรณี + ทดสอบหน้าจอจริง")]
for i, (n, t, d) in enumerate(steps):
    y = 1.50 + i * 0.92
    box(s, 0.85, y, 0.62, 0.62, NAVY, NAVY, adj=0.2)
    txt(s, 0.85, y + 0.18, 0.62, 0.26, n, 14, True, WHITE, PP_ALIGN.CENTER)
    txt(s, 1.70, y + 0.08, 2.4, 0.3, t, 15, True, NAVY)
    txt(s, 4.20, y + 0.12, 4.2, 0.5, d, 12, False, INK, line=1.25)
card(s, 8.70, 1.50, 3.95, 4.15, GREEN, "หลักฐานการพัฒนา (ของจริง)",
     "• โค้ด 34 ไฟล์ Dart รวม 6,319 บรรทัด\n"
     "• ชุดทดสอบ 6 ไฟล์ 646 บรรทัด รวม 45 กรณี\n"
     "• 10 commit ตั้งแต่ 4 มิ.ย. – 16 ก.ย. 2569\n"
     "• 9 หน้าจอ · แม่แบบ habit 18 รายการ 3 หมวด\n"
     "• ไฟล์ติดตั้ง app-release.apk ขนาด 60.8 MB\n"
     "• คู่มือติดตั้ง SETUP.md ที่ผู้อื่นทำตามแล้วรันได้", 11.5, title_size=15)

# ---------------------------------------------------------------- 7 result
s = page()
head(s, "06", "ผลการพัฒนา ณ สถานะ 100%",
     "แอปใช้งานได้จริงครบทุกฟังก์ชัน ติดตั้งด้วยไฟล์ APK บนมือถือ Android",
     "ฟังก์ชันครบ 9/9 พร้อมให้ตรวจ")
box(s, 0.90, 1.50, 2.00, 2.00, GREENBG, GREEN, adj=0.08)
txt(s, 1.00, 2.05, 1.80, 0.50, "100%", 32, True, GREEN, PP_ALIGN.CENTER)
txt(s, 1.05, 2.65, 1.70, 0.30, "ฟังก์ชัน 9/9", 10.5, True, GREEN, PP_ALIGN.CENTER)
card(s, 3.25, 1.50, 4.40, 2.10, GREEN, "บันทึกพฤติกรรมประจำวัน", "")
txt(s, 4.95, 2.12, 2.50, 1.30,
    "เลือกวันจากแถบวันที่\nกดเช็ค habit · กรอกค่าตัวเลข\nแถบสรุปความคืบหน้าของวัน",
    12, False, INK, line=1.3)
card(s, 7.95, 1.50, 4.40, 2.10, TEAL, "สถิติและความต่อเนื่อง", "")
txt(s, 10.30, 2.12, 1.95, 1.30,
    "Streak ปัจจุบัน / สูงสุด\nปฏิทินย้อนหลัง 1 ปี\nกราฟแท่งรายสัปดาห์",
    12, False, INK, line=1.3)
picture(s, shot("01_dashboard.png"), 3.48, 2.12, 1.30)
picture(s, shot("02_analytics.png"), 8.18, 2.12, 1.30)
box(s, 0.90, 4.05, 11.45, 1.60, WHITE, BORDER, adj=0.06)
txt(s, 1.20, 4.32, 2.00, 0.30, "ประโยคที่ควรพูด", 15.5, True, NAVY)
txt(s, 3.00, 4.26, 8.75, 0.72,
    "“จากขอบเขต 9 ฟังก์ชัน พัฒนาเสร็จครบ 9 ฟังก์ชัน คิดเป็น 100% "
    "ผ่านชุดทดสอบอัตโนมัติ 45 กรณี และสร้างไฟล์ APK ติดตั้งบนมือถือจริงได้แล้ว”",
    13.2, False, INK, line=1.35)
txt(s, 3.00, 5.08, 8.75, 0.30,
    "ภาพหน้าจอจากแอปที่รันจริง (ข้อมูลตัวอย่าง) · โค้ดชุดเดียวกับไฟล์ APK",
    11.5, True, RED)

# ---------------------------------------------------------------- 8 screens
s = page()
head(s, "07", "หน้าจอที่พัฒนาเสร็จแล้ว",
     "ภาพจากแอปที่รันจริงด้วยข้อมูลตัวอย่าง ครอบคลุมทุกฟังก์ชันหลัก",
     "ภาพหน้าจอจริงทุกฟังก์ชัน")
shots = [("01_dashboard.png", "หน้าหลัก", "บันทึกรายวัน · ค่าตัวเลข"),
         ("02_analytics.png", "สถิติ", "Streak · ปฏิทิน 1 ปี"),
         ("04_add_habit.png", "เพิ่ม habit", "ความถี่ · เวลาเตือน"),
         ("05_templates.png", "แม่แบบ 18 รายการ", "3 หมวด เลือกแล้วใช้ได้ทันที"),
         ("07_categories.png", "จัดการหมวดหมู่", "เพิ่ม · แก้ไข · ลบ"),
         ("06_settings.png", "ตั้งค่า", "ธีม · ส่งออกข้อมูล")]
for i, (f, t, d) in enumerate(shots):
    x = 0.70 + (i % 6) * 2.06
    picture(s, shot(f), x + 0.32, 1.60, 2.60)
    txt(s, x, 4.35, 1.95, 0.28, t, 12.5, True, NAVY, PP_ALIGN.CENTER)
    txt(s, x, 4.65, 1.95, 0.5, d, 10, False, GREY, PP_ALIGN.CENTER, line=1.2)
note(s, 0.70, 5.45, 11.95, 1.10, "หมายเหตุ",
     "ภาพทั้งหมดแคปจากแอปที่รันจริงด้วยโค้ดชุดเดียวกับไฟล์ APK · ข้อมูลในภาพเป็นข้อมูลตัวอย่างที่สร้างไว้สาธิต",
     12)

# ---------------------------------------------------------------- 9 demo
s = page()
head(s, "08", "Demo Scenario — ลำดับการสาธิต",
     "สาธิตบนมือถือจริงหรืออีมูเลเตอร์ ใช้เวลา 2–3 นาที",
     "เพิ่ม → บันทึก → สถิติ → ส่งออก")
demo = [("A", "เตรียมระบบ", "ติดตั้งแอปจากไฟล์ APK · เตรียมข้อมูลตัวอย่างที่มี Streak หลายวัน"),
        ("B", "เพิ่ม habit", "กดปุ่มเพิ่ม → เลือกจากแม่แบบ 18 รายการ → ตั้งความถี่และเวลาแจ้งเตือน"),
        ("C", "บันทึกประจำวัน", "กดเช็ค habit ของวันนี้ · กรอกค่าตัวเลข เช่น ดื่มน้ำ 7 จาก 8 แก้ว"),
        ("D", "ดูสถิติ", "เปิดหน้าสถิติ → Streak ปัจจุบัน/สูงสุด → ปฏิทิน 1 ปี → กราฟรายสัปดาห์"),
        ("E", "ส่งออกและตรวจ", "ตั้งค่า → ส่งออก CSV/JSON → สลับธีมมืด → ปิดแอปเปิดใหม่ ข้อมูลยังอยู่")]
for i, (n, t, d) in enumerate(demo):
    y = 1.50 + i * 0.95
    c = GREEN if i == len(demo) - 1 else NAVY
    box(s, 0.85, y, 0.72, 0.72, c, c, adj=0.15)
    txt(s, 0.85, y + 0.22, 0.72, 0.28, n, 15, True, WHITE, PP_ALIGN.CENTER)
    txt(s, 1.80, y + 0.08, 2.4, 0.3, t, 15, True, NAVY)
    txt(s, 4.20, y + 0.12, 8.2, 0.5, d, 12.2, False, INK, line=1.25)
txt(s, 4.20, 6.40, 8.2, 0.3,
    "เตรียมวิดีโอสาธิตสำรองไว้ด้วย เผื่อเครื่องสาธิตมีปัญหาหน้างาน", 11.5, True, AMBERTX)

# ---------------------------------------------------------------- 10 test plan
s = page()
head(s, "09", "แผนการทดสอบและวิธีประเมิน",
     "ทดสอบ 5 ด้าน แต่ละด้านมีวิธีทดสอบและเกณฑ์ผ่านชัดเจน",
     "Test case ชัด + เกณฑ์ผ่านชัด")
table(s, 0.70, 1.50, 11.95,
      ["ประเภททดสอบ", "วิธีทดสอบ", "เกณฑ์ผ่าน"],
      [["Unit Test", "ตัวคำนวณ Streak · ความถี่รายสัปดาห์ · ตัวส่งออกข้อมูล · ตัวตั้งเวลาแจ้งเตือน",
        "ผ่านทุกกรณีที่กำหนด"],
       ["Widget Test", "การ์ด habit แสดงชื่อ ค่าตัวเลข และสถานะถูกต้อง", "ผ่านทุกกรณี"],
       ["Database Test", "เพิ่ม–แก้–ลบหมวดหมู่ และอ่าน–เขียน log บนฐานข้อมูลจริง", "ข้อมูลตรงทุกกรณี"],
       ["UI Test (อัตโนมัติ)", "สั่งเบราว์เซอร์กดแอปจริง: บันทึก · Streak · ค่าตัวเลข · ธีม", "ผลตรงกับที่คาดไว้"],
       ["Build / Deploy", "สร้างไฟล์ APK แบบ release แล้วตรวจขนาดและการติดตั้ง", "สร้างสำเร็จ ติดตั้งได้"]],
      [2.3, 6.6, 3.05], row_h=0.62, body_size=11.5)
card(s, 0.70, 5.10, 5.85, 1.45, TEAL, "เครื่องมือที่ใช้",
     "flutter test (ชุดทดสอบในโค้ด) · Playwright สั่งเบราว์เซอร์กดแอปจริง · flutter build apk",
     11.5, title_size=14)
card(s, 6.80, 5.10, 5.85, 1.45, AMBER, "ข้อจำกัดของการทดสอบ",
     "แจ้งเตือนทดสอบด้วยชุดทดสอบการตั้งเวลา ยังไม่ได้ทดสอบเสียงเตือนเด้งบนมือถือจริง",
     11.5, title_size=14)

# ---------------------------------------------------------------- 11 test result
s = page()
head(s, "10", "ผลการทดสอบและตัวชี้วัด",
     "ผลวัดจริงจากการรันทดสอบ ไม่ใช่ค่าประมาณ",
     "ตัวเลข + วิธีวัด + วันที่วัด")
txt(s, 0.70, 1.38, 4.0, 0.3, "ผลการทดสอบจริง", 15.5, True, NAVY)
table(s, 0.70, 1.75, 8.0,
      ["รายการทดสอบ", "เกณฑ์", "ผลจริง", "ผล"],
      [["ชุดทดสอบทั้งหมด (flutter test)", "ผ่านทุกกรณี", "45 / 45", ("ผ่าน", GREEN, True)],
       ["ตัวคำนวณ Streak", "ตรงทุกกรณี", "ผ่านทุกกรณี", ("ผ่าน", GREEN, True)],
       ["ความถี่ N ครั้งต่อสัปดาห์", "นับเป็นสัปดาห์", "ผ่านทุกกรณี", ("ผ่าน", GREEN, True)],
       ["บันทึกแล้วปิด–เปิดแอปใหม่", "ข้อมูลคงอยู่", "2/5 → 3/5 คงอยู่", ("ผ่าน", GREEN, True)],
       ["เช็คติดกัน 3 วัน แล้วดู Streak", "แสดงค่า 3", "3 วัน", ("ผ่าน", GREEN, True)],
       ["habit เชิงตัวเลข (ดื่มน้ำ)", "กราฟตรงค่าที่กรอก", "7/8 แก้ว · กราฟ 7", ("ผ่าน", GREEN, True)],
       ["ธีมมืดหลังปิด–เปิดแอป", "ยังเป็นธีมมืด", "ยังเป็นธีมมืด", ("ผ่าน", GREEN, True)],
       ["สร้างไฟล์ติดตั้ง APK", "สร้างสำเร็จ", "60.8 MB", ("ผ่าน", GREEN, True)]],
      [3.4, 1.65, 1.85, 1.1], row_h=0.43, body_size=10.5)
card(s, 9.05, 1.75, 3.60, 4.55, TEAL, "รายละเอียดการทดสอบ",
     "• วันที่วัด: 21 กันยายน 2569\n"
     "• ชุดทดสอบในโค้ด 6 ไฟล์ 646 บรรทัด สั่งรันด้วย flutter test\n"
     "• ทดสอบหน้าจอ: สั่งเบราว์เซอร์กดแอปจริงแบบอัตโนมัติ เก็บภาพหน้าจอทุกขั้น\n"
     "• ภาพหลักฐานอยู่ในโฟลเดอร์ docs/test_evidence\n"
     "• ผลทดสอบตอบวัตถุประสงค์ครบทั้ง 3 ข้อ", 11.2, title_size=15)

# ---------------------------------------------------------------- 12 evidence type
s = page()
head(s, "11", "หลักฐานที่ต้องแสดงตามประเภทโครงงาน",
     "โครงงานประเภท Mobile Application ใช้หลักฐานกลุ่ม M เป็นหลัก",
     "หลักฐานตรงกับประเภทงาน")
card(s, 0.70, 1.50, 5.85, 4.60, RGBColor(0x2E, 0x75, 0xB6), "M — Mobile Application",
     "• ไฟล์ติดตั้ง APK ที่ลงบนมือถือจริงได้\n"
     "• หน้าจอครบ 9 หน้า พร้อมภาพหน้าจอทุกหน้า\n"
     "• ฐานข้อมูลในเครื่อง SQLite 3 ตาราง\n"
     "• การแจ้งเตือนตามเวลาที่ผู้ใช้ตั้ง\n"
     "• ชุดทดสอบอัตโนมัติ 45 กรณี\n"
     "• ใช้งานออฟไลน์ ข้อมูลไม่ออกจากเครื่อง\n"
     "• ส่งออกข้อมูลเป็นไฟล์ CSV / JSON ได้", 12, title_size=16)
card(s, 6.80, 1.50, 5.85, 4.60, GREY, "สิ่งที่โครงงานนี้ไม่ได้ทำ",
     "• ไม่ได้ใช้ AI หรือโมเดลเรียนรู้ของเครื่อง\n"
     "• ไม่มีเซิร์ฟเวอร์หรือ API ภายนอก\n"
     "• ไม่มีวงจรหรือฮาร์ดแวร์ จึงไม่มี Schematic / PCB\n"
     "• ไม่มีระบบสมาชิก จึงไม่มีตารางสิทธิ์ผู้ใช้\n\n"
     "ตัวเลขสถิติทั้งหมดคำนวณจากข้อมูลที่ผู้ใช้บันทึกเอง ไม่มีการสุ่มหรือประมาณค่า",
     12, title_size=16)

# ---------------------------------------------------------------- 13 problems
s = page()
head(s, "12", "ปัญหาที่พบ การแก้ไข และข้อจำกัด",
     "ปัญหาจริงระหว่างพัฒนา พร้อมสาเหตุ วิธีแก้ และผลหลังแก้",
     "ปัญหา → สาเหตุ → วิธีแก้ → ผล")
table(s, 0.70, 1.45, 11.95,
      ["ปัญหาที่พบ", "สาเหตุ", "วิธีแก้", "ผลหลังแก้"],
      [["Streak แสดง 1 วัน ทั้งที่ทำติดกัน 9 วัน",
        "ตัวคำนวณเอาช่วงที่ทำติดกันซึ่งเก่ากว่า มาเขียนทับค่าปัจจุบัน",
        "หยุดนับค่าปัจจุบันทันทีที่เจอวันแรกที่ขาด",
        "ค่าตรงกับข้อมูลจริงทุกกรณี"],
       ["ไอคอนแม่แบบขึ้นเป็นรูปดาวทั้ง 18 รายการ",
        "รหัสไอคอนเขียนเป็นตัวเลขตายตัว ไม่ตรงกับไอคอนในรายการ",
        "ผูกกับชื่อไอคอนจริง และเพิ่มไอคอนที่ขาดไป 6 ตัว",
        "ไอคอนตรงกับชื่อแม่แบบทุกรายการ"],
       ["ปฏิทินย้อนหลัง 1 ปี เปิดมาเห็นแต่ช่องว่าง",
        "ปฏิทินเริ่มที่ปีที่แล้วทางซ้าย วันล่าสุดอยู่นอกจอ",
        "กำหนดให้เลื่อนมาที่ฝั่งวันล่าสุดตั้งแต่เปิดหน้า",
        "เห็นข้อมูลทันทีที่เปิดหน้าสถิติ"],
       ["คอมไพล์ไม่ผ่าน หาคลาส Habit ไม่เจอ",
        "drift ต้องสร้างไฟล์โค้ดอัตโนมัติก่อน จึงจะมีคลาสให้เรียกใช้",
        "สั่ง build_runner ทุกครั้งที่แก้ตาราง และบันทึกขั้นตอนไว้ใน SETUP.md",
        "คอมไพล์ผ่าน เพื่อนทำตามคู่มือได้"]],
      [2.9, 3.1, 3.3, 2.65], row_h=0.78, body_size=10.5)
card(s, 0.70, 5.25, 5.85, 1.30, AMBER, "ข้อจำกัดที่ยังเหลือ",
     "ยังไม่ได้ทดสอบการแจ้งเตือนบนมือถือจริง · ยังไม่มีการซิงก์ข้ามเครื่อง",
     11.5, title_size=14)
card(s, 6.80, 5.25, 5.85, 1.30, TEAL, "แนวทางพัฒนาต่อ",
     "ทดสอบกับผู้ใช้จริงและเก็บความพึงพอใจ · สำรองข้อมูลขึ้นคลาวด์ · เผยแพร่ขึ้น Play Store",
     11.5, title_size=14)

# ---------------------------------------------------------------- 14 deliverables
s = page()
head(s, "13", "ผลส่งมอบของโครงงาน",
     "สิ่งที่กรรมการตรวจรับได้หลังการสอบ",
     "ของจริง + เอกสาร + Source")
items = [("แอปที่ติดตั้งได้จริง", "app-release.apk ขนาด 60.8 MB · ติดตั้งบนมือถือ Android"),
         ("Source Code", "Git 10 commit · โค้ด 34 ไฟล์ Dart รวม 6,319 บรรทัด"),
         ("ฐานข้อมูล / โครงสร้าง", "SQLite 3 ตาราง · โค้ดสร้างตารางอยู่ใน repository"),
         ("เอกสารโครงงาน", "คู่มือติดตั้ง SETUP.md · README · เอกสารนำเสนอชุดนี้"),
         ("หลักฐานทดสอบ", "ผล flutter test 45 กรณี · ภาพหน้าจอผลทดสอบทุกขั้น")]
for i, (t, d) in enumerate(items):
    y = 1.45 + i * 0.92
    box(s, 0.85, y, 0.55, 0.55, GREEN, GREEN, adj=0.2)
    txt(s, 0.85, y + 0.13, 0.55, 0.3, "✓", 20, True, WHITE, PP_ALIGN.CENTER, font="Arial")
    txt(s, 1.70, y + 0.06, 2.65, 0.28, t, 14.5, True, NAVY)
    txt(s, 4.45, y + 0.06, 4.0, 0.6, d, 12.2, False, INK, line=1.25)
ph = picture_w(s, shot("03_evidence_test.png"), 8.70, 1.60, 3.95)
txt(s, 8.70, 1.75 + ph, 3.95, 0.3, "ผลรัน flutter test จริง", 11.5, True, GREY, PP_ALIGN.CENTER)

# ---------------------------------------------------------------- 15 summary
s = page()
head(s, "14", "สรุปผลเทียบกับวัตถุประสงค์",
     "หลักฐานว่าวัตถุประสงค์ทั้ง 3 ข้อบรรลุครบ",
     "วัตถุประสงค์ → หลักฐาน → สถานะ")
table(s, 0.70, 1.50, 11.95,
      ["วัตถุประสงค์", "หลักฐาน / ผลที่ได้", "สถานะ"],
      [["ข้อ 1: บันทึกพฤติกรรมประจำวันแบบออฟไลน์ ไม่ต้องสมัครสมาชิก",
        "บันทึกลง SQLite ในเครื่อง · ปิดแอปแล้วเปิดใหม่ข้อมูลยังอยู่ (ทดสอบแล้ว)",
        ("บรรลุ", GREEN, True)],
       ["ข้อ 2: คำนวณ Streak อัตราสำเร็จ และปฏิทินย้อนหลัง 1 ปี",
        "หน้าสถิติแสดงครบ · ชุดทดสอบตัวคำนวณผ่านทุกกรณี",
        ("บรรลุ", GREEN, True)],
       ["ข้อ 3: รองรับความถี่หลายแบบ แจ้งเตือน และส่งออกข้อมูล",
        "รายวัน / เลือกวัน / N ครั้งต่อสัปดาห์ · ตั้งเวลาเตือน · ส่งออก CSV–JSON",
        ("บรรลุ", GREEN, True)]],
      [4.5, 6.1, 1.35], row_h=0.80, body_size=11.5)
box(s, 0.70, 4.60, 11.95, 1.40, GREENBG, GREEN, adj=0.12)
txt(s, 1.10, 4.95, 11.15, 0.75,
    "“โครงงานพัฒนาได้ครบตามขอบเขตทั้ง 9 ฟังก์ชัน ผ่านชุดทดสอบอัตโนมัติ 45 กรณี "
    "และสร้างไฟล์ติดตั้งจริงได้แล้ว จึงถือว่าดำเนินงานสำเร็จ 100% ของขอบเขตที่กำหนด”",
    14.5, True, RGBColor(0x1B, 0x5E, 0x3F), PP_ALIGN.CENTER, line=1.35)

# ---------------------------------------------------------------- 16 Q&A
s = dark()
box(s, 0, 0, 13.33, 7.5, NAVY, None, MSO_SHAPE.RECTANGLE, None)
txt(s, 0.80, 1.15, 3.2, 0.75, "Q&A", 44, True, WHITE, font="Aptos Display")
txt(s, 0.82, 2.15, 7.2, 0.5, "พร้อมสาธิตแอป และตอบคำถามจากหลักฐานจริง", 20, False, PALE)
box(s, 0.82, 3.05, 7.30, 2.25, NAVY3, None, adj=0.06)
txt(s, 1.15, 3.36, 2.8, 0.34, "พร้อมให้ตรวจสอบ", 17, True, ICE)
txt(s, 1.15, 3.82, 6.5, 1.20,
    "ติดตั้งจากไฟล์ APK บนมือถือได้ทันที\n"
    "Source code · ชุดทดสอบ 45 กรณี · ภาพหลักฐาน เปิดตรวจได้\n"
    "พัฒนาเองทั้งระบบ: วิเคราะห์ → ออกแบบ → พัฒนา → ทดสอบ → สร้างไฟล์ติดตั้ง\n"
    "ทุกตัวเลขในสไลด์วัดจากการรันจริง", 12.4, False, WHITE, line=1.35, space=3)
box(s, 9.30, 1.15, 2.65, 2.65, NAVY2, None, MSO_SHAPE.OVAL, None)
txt(s, 9.35, 2.00, 2.55, 0.52, "100%", 34, True, WHITE, PP_ALIGN.CENTER)
txt(s, 9.18, 4.20, 2.90, 0.36, "READY TO DEFEND", 13, True, ICE, PP_ALIGN.CENTER, font="Aptos")
txt(s, 8.60, 5.20, 4.10, 0.42, "Habit Tracker · ภูตะวัน ทองวิลาศ", 14, False, WHITE, PP_ALIGN.CENTER)

out = os.path.join(HERE, "นำเสนอ100_HabitTracker.pptx")
prs.save(out)
print("SAVED:", out)
print("slides:", len(prs.slides._sldIdLst))
